import React from 'react';
import AppBarMargin from './AppBarMargin';
import GoogleDrive from './GoogleDrive';

const Sync = () => (
  <AppBarMargin>
    <GoogleDrive />
  </AppBarMargin>
);

export default Sync;
